// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build
export default defineConfig({
  site: 'https://velidion.com',
  // Static output — deploys anywhere (Cloudflare Pages, Vercel, Netlify, GitHub Pages).
});
