/** Single source of truth for values duplicated across components
 *  (brand name, contact email) — update here, not per-file. */
export const SITE = {
  name: "Velidion",
  contactEmail: "hello@velidion.com",
} as const;
